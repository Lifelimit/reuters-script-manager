SNYK COMPARE SCRIPT - COMPLETE USER GUIDE
==========================================

TABLE OF CONTENTS
==================

- Overview: Script introduction and key benefits
- What the Script Does: Core functionality overview
- Prerequisites: Required files and packages
- Installation & Setup: Step-by-step installation guide
- How to Use the Script: Running the script instructions
- Understanding the Output: Output files and features
- Troubleshooting: Common issues and solutions
- File Structure: Input/output file organization
- Advanced Features: Customization options
- Best Practices: Recommended workflows

================================================================================

QUICK START GUIDE
=================

QUICK STEPS TO RUN THE SCRIPT
------------------------------

METHOD 1: USING REUTERS SCRIPT MANAGER (RECOMMENDED)
-----------------------------------------------------
Step 1: Launch the Reuters Script Manager
- Windows: `python script_launcher.py`
- macOS/Linux: `python3 script_launcher.py`
- Wait for the automatic setup to complete on the first launch.

Step 2: Select the Snyk Script
- The script will automatically appear in the dropdown menu as "Snyk Report Compare/snyk_compare.py".

Step 3: Prepare Your Files
- Place your Snyk report files (e.g., `Snyk Report CAT YYYY-MM-DD.xlsx`) in the `Snyk Report Compare` folder. The script needs at least two reports to compare.
- Place your `Snyk Vulnerability tracker.xlsx` file in the same folder.
- Make sure the files are not open in Excel.

Step 4: Run the Script
- Click the prominent blue "🚀 Run Script" button.
- The script will automatically find the two most recent reports and the latest tracker file to process.
- View real-time progress in the launcher's output window.
- Colored indicators: Successful steps show a green ✓, warnings/errors show a red ✗ in the output.

Step 5: Review Results
- Check Output: Review the modified new report Excel file. It will contain a "Working sheet" and a "Vulnerability Tracker" sheet.
- Use Templates: Use the generated `...-Ticket-Template.txt` file to create new tickets.
- Archived Files: The original input files are automatically moved to the `_archive` folder.

METHOD 2: MANUAL EXECUTION (ALTERNATIVE)
-----------------------------------------
Step 1: Prepare Files
- Place at least two Snyk reports and the tracker file in the same folder as the script.
- Ensure the files are not open in Excel.
- Verify that report filenames contain dates in `YYYY-MM-DD` format for correct identification.

Step 2: Run the Script
1. Check Dependencies: Run `python check_dependencies.py` to ensure your environment is set up.
2. Execute Script: Run `python snyk_compare.py` from your terminal.
3. The script will automatically locate and process the latest files.
- Console colorization: Colored ✓/✗ are enabled in console output; if your terminal strips ANSI colors, you'll still see [OK]/[MISSING].

Step 3: Review Results
- Check the modified Excel report for new vulnerabilities.
- Use the ticket templates for new vulnerabilities.
- The processed input files will be moved to the `_archive` folder.

Pro Tip: The Reuters Script Manager provides a much better user experience with real-time output, automatic dependency management, and a professional, platform-native interface.

================================================================================

OVERVIEW
========

The Snyk Compare Script is a powerful Python tool designed to analyze and compare Snyk vulnerability reports. It helps security teams identify new vulnerabilities, track remediation progress, and maintain comprehensive vulnerability management workflows.

KEY BENEFITS
------------

- Automated Comparison: Identifies new vulnerabilities between report periods.
- Ticket Integration: Cross-references with existing vulnerability tracker.
- Professional Output: Creates formatted Excel reports with hyperlinks and conditional formatting.
- Template Generation: Automatically creates ticket templates for new vulnerabilities.
- File Management: Organizes and archives input files automatically.

================================================================================

WHAT THE SCRIPT DOES
====================

PRIMARY FUNCTIONS
-----------------

1. Report Comparison
   - Compares two Snyk vulnerability reports (old vs. new).
   - Identifies new vulnerabilities that weren't in the previous report.
   - Uses ISSUE_URL as the unique identifier for accurate matching.

2. Data Processing
   - Sorts vulnerabilities alphabetically by name.
   - Counts critical severity vulnerabilities.
   - Filters out Docker-related projects (configurable).
   - Extracts and formats CVE/CWE information.

3. Tracker Integration
   - Cross-references new vulnerabilities with existing tracker file.
   - Maps existing tickets to vulnerabilities.
   - Appends new vulnerabilities without tickets to the tracker.

4. Output Generation
   - Creates a "Working sheet" with new vulnerabilities.
   - Generates a "Vulnerability Tracker" sheet.
   - Applies professional formatting and hyperlinks.
   - Creates ticket templates for vulnerabilities without existing tickets.

5. File Management
   - Backs up original files.
   - Archives processed files in organized folders.
   - Maintains file integrity throughout the process.

================================================================================

PREREQUISITES
=============

REQUIRED FILES
--------------
Place the following files in the `Snyk Report Compare` directory. The script will automatically find the correct files to process.

- Snyk Reports: At least two Snyk report Excel files (e.g., `Snyk Report CAT YYYY-MM-DD.xlsx`). The script will compare the two most recent ones based on the date in the filename.
- Vulnerability Tracker: An Excel file containing tracker information (e.g., `Snyk Vulnerability tracker.xlsx`). The script will use the most recently modified one. This file is optional.

REQUIRED PYTHON PACKAGES
------------------------
- `pandas>=2.0.0`
- `openpyxl>=3.1.0`
- `Pillow>=8.0.0`

================================================================================

INSTALLATION & SETUP
====================

USING REUTERS SCRIPT MANAGER (RECOMMENDED - AUTOMATIC SETUP)
-------------------------------------------------------------

The Reuters Script Manager handles all setup automatically:

1. Run the Reuters Script Manager: 
   - Windows: python script_launcher.py
   - macOS/Linux: python3 script_launcher.py
2. Wait for automatic setup:
   - Creates virtual environment.
   - Installs all dependencies.
   - Verifies tkinter availability.

   - Applies platform-specific styling.
3. Ready to use! No manual setup required

The Reuters Script Manager automatically:
- Detects your platform (Windows, macOS, Linux).
- Installs tkinter on Apple Silicon Macs and Linux.
- Creates and manages virtual environment.
- Installs all required Python packages.
- Provides real-time setup progress.
- Applies native platform-specific design.
- Detects and adapts to system dark/light mode.
- Provides interactive tooltips for all controls.
- Performs real-time dependency version checking.
- Displays enhanced script information with live status.

MANUAL SETUP (ALTERNATIVE)
---------------------------

If you prefer manual setup or need to troubleshoot:

STEP 1: CHECK DEPENDENCIES
---------------------------

Before running the main script, always run the dependency checker:

For most systems:
    # On macOS/Linux:
    python3 check_dependencies.py

    # On Windows:
    python check_dependencies.py

The dependency checker will:
- Detect your platform automatically.
- Check if required packages are installed.
- Offer to install missing dependencies.
- Create a virtual environment if needed.

STEP 2: INSTALL DEPENDENCIES (if needed)
-----------------------------------------

If dependencies are missing, the script will offer automatic installation:

For Windows:
1. Install Python from https://python.org (if not already installed)
2. Run: pip install -r requirements.txt

For macOS (Intel Mac):
1. Install Homebrew: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
2. Install Python: brew install python
3. Install tkinter: brew install python-tk
4. Install dependencies: pip3 install -r requirements.txt

For macOS (Apple Silicon M1/M2/M3):
1. Install Homebrew: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
2. Install Python: brew install python
3. Install tkinter: brew install python-tk
4. Install dependencies: pip3 install -r requirements.txt

For Linux:
The script automatically detects and uses your package manager:
- Ubuntu/Debian: sudo apt update && sudo apt install -y python3 python3-pip python3-tk
- CentOS/RHEL: sudo yum install -y python3 python3-pip tkinter
- Fedora: sudo dnf install -y python3 python3-pip tkinter
- Arch Linux: sudo pacman -S --noconfirm python python-pip tk
- openSUSE: sudo zypper install -y python3 python3-pip python3-tk

VIRTUAL ENVIRONMENT (Recommended)
---------------------------------

1. Create virtual environment: python -m venv snyk_env
2. Activate virtual environment:
   - Windows: snyk_env\Scripts\activate
   - macOS/Linux: source snyk_env/bin/activate
3. Install dependencies: pip install -r requirements.txt
4. Run script: 
   - Windows: python snyk_compare.py
   - macOS/Linux: python3 snyk_compare.py

================================================================================

HOW TO USE THE SCRIPT
=====================

STEP-BY-STEP INSTRUCTIONS
-------------------------

1. PREPARE YOUR FILES
   - Place your Snyk report Excel files in the `Snyk Report Compare` folder. Ensure filenames contain a date (e.g., `Snyk Report CAT 2025-08-29.xlsx`).
   - Place your `Snyk Vulnerability tracker.xlsx` file in the same folder.
   - Make sure none of the files are open in another program like Excel.

2. RUN THE SCRIPT (via Script Launcher)
   - Launch the Reuters Script Manager.
   - Select "Snyk Report Compare/snyk_compare.py" from the dropdown.
   - Click the "🚀 Run Script" button.

3. AUTOMATIC FILE DISCOVERY
   - The script automatically identifies the two most recent Snyk reports by parsing the dates in their filenames.
   - It finds the latest vulnerability tracker file by checking the last modified time.

4. REVIEW THE OUTPUT
   - Open the newest of your Snyk report files. It will now contain the new "Working sheet" and "Vulnerability Tracker" sheet.
   - A `...-Ticket-Template.txt` file will be created in the script's directory for any new vulnerabilities that need tickets.
   - The original input files will be moved to a new folder under `_archive/`.

================================================================================

UNDERSTANDING THE OUTPUT
========================

OUTPUT FILES CREATED
--------------------

1. Modified New Report
   - Location: Same as your selected new report file.
   - Sheets Created:
     - Original Sheet: Your original data (renamed to include "-original").
     - Working Sheet: New vulnerabilities only (green tab).
     - Vulnerability Tracker: Formatted tracker data (light blue tab).

2. Working Sheet Features
   - New Vulnerabilities Only: A clean list of what's new.
   - Ticket Column: Shows links to existing tickets from your tracker file.
   - Hyperlinked URLs: `ISSUE_URL` values are clickable links with consistent formatting.
   - Conditional Formatting: 'Critical' vulnerabilities are highlighted in red.
   - Auto-Filter: All columns are filterable for easy data exploration.
   - Adjusted Column Widths: Columns are resized for better readability.

3. Vulnerability Tracker Sheet
   - Report Date: Automatically extracted from the new report's filename.
   - Ticket Information: Includes links to existing tickets.
   - Formatted Data: Dates and other fields are properly formatted.
   - Docker Projects Excluded: Provides a cleaner list for tracking.

4. Ticket Template File
   - Filename: `[NewReportName]-Ticket-Template.txt`
   - Content: Pre-formatted text blocks for each new vulnerability that doesn't have a ticket yet.
   - Format: Ready to copy and paste into your ticketing system.

5. Archive Folder
   - Location: `_archive/Snyk Report - YYYY-MM-DD/`.
   - Contents: Your original input files (the two Snyk reports and the tracker) are moved here.

KEY COLUMNS IN OUTPUT
=====================

- TICKET: Links to existing tickets (if any).
- ID: The unique vulnerability identifier.
- NAME: The vulnerability name.
- TR SEVERITY: The severity level (Critical, High, etc.).
- PROJECT_NAME: The GitHub repository name.
- PROBLEM_TITLE: A detailed vulnerability description.
- CVE/CWE: CVE and CWE identifiers.
- PACKAGE_NAME_AND_VERSION: The affected library and version.
- GRACE_PERIOD: The remediation deadline.
- ISSUE_URL: A link to the Snyk vulnerability details.

================================================================================

TROUBLESHOOTING
===============

COMMON ISSUES AND SOLUTIONS
---------------------------

1. "Required column 'ISSUE_URL' not found"
   - Problem: Your Snyk report does not have the expected column structure.
   - Solution: Ensure your Snyk report export includes the `ISSUE_URL` column.

2. "Could not find sheet 'Snyk all repositories'"
   - Problem: The script cannot find the required sheet in your Excel file.
   - Solution: Ensure your Snyk report has a sheet named "Snyk all repositories".

3. "No new vulnerabilities were found"
   - Problem: The new report contains no vulnerabilities that were not in the old report.
   - Solution: This is a normal outcome if no new issues were found.

4. Python/Package Installation Issues
   - Problem: Missing required Python packages.
   - Solution: Use the `check_dependencies.py` script or the Reuters Script Manager to automatically install dependencies.

5. File Permission Errors
   - Problem: The script cannot write to the output file or create the archive directory.
   - Solution: Ensure you have write permissions in the script's directory and that the input files are not open in another program like Excel.

6. Memory Issues with Large Reports
   - Problem: Script runs out of memory with very large reports.
   - Solution: Close other applications, increase virtual memory, or process reports in smaller batches.

7. Excel File Corruption
   - Problem: Output Excel file is corrupted or won't open.
   - Solution: Check that input files aren't corrupted, ensure sufficient disk space.

8. Date Extraction Errors
   - Problem: The script can't extract dates from report filenames to determine which are newest.
   - Solution: Ensure report filenames contain dates in `YYYY-MM-DD` or `YYYY_MM_DD` format.

9. Odd characters (�) in output
   - Problem: Some third‑party tools emit non‑UTF‑8 bytes.
   - Solution: When run via the Reuters Script Manager, output is forced to UTF‑8 and undecodable bytes are safely replaced so the app doesn’t crash. If you run from a terminal, ensure your console is UTF‑8 or leave the defaults; no further action is needed.

VIRTUAL ENVIRONMENT ISSUES
--------------------------

- Permission errors: Run as administrator (Windows) or with sudo (Unix).
- Python not found: Ensure Python is installed and in PATH.
- Activation fails: Check the correct activation script path.

PACKAGE INSTALLATION ISSUES
---------------------------

- Network errors: Check internet connection.
- Permission errors: Use --user flag: pip install --user -r requirements.txt.
- Version conflicts: Use virtual environment to isolate packages.

PLATFORM-SPECIFIC ISSUES
------------------------

- macOS: Ensure Xcode command line tools are installed.
- Windows: Ensure Microsoft Visual C++ Build Tools are installed.
- Linux: Install python3-dev and build-essential packages.

================================================================================


================================================================================

BEST PRACTICES
==============

FILE NAMING
-----------
- Use consistent naming conventions for your reports.
- Include dates in filenames (YYYY-MM-DD format).
- Keep tracker files in a consistent location.

REGULAR PROCESSING
------------------
- Run the script regularly (weekly or bi-weekly).
- Keep a consistent schedule for report generation.
- Archive old reports systematically.
